let calculator = {
  add: (a, b) => a + b,
  subtract: (a, b) => a - b,
  multiply: (a, b) => a * b,
  divide: (a, b) => a / b,
};

let addResult = calculator.add(10, 5);
console.log(addResult);
let divideResult = calculator.divide(10, 5);
console.log(divideResult);
